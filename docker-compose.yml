version: '3.8'

services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
    networks:
      - new
    command: air ./cmd/main.go -b 0.0.0.0
    depends_on:
      - postgres

  postgres:
    image: postgres:alpine
    networks:
      new:
        aliases:
          - database
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "root"
      POSTGRES_DB: "task-management-db"
    ports:
      - "5432:5432"
    volumes:
      - postgres-db:/var/lib/postgresql/data

  # migrate:
  #   image: migrate/migrate
  #   networks:
  #       - new
  #   volumes:
  #       - /c/Users/thuy.nguyen/go/task-management-app/db/migrations:/migrations
  #   command: ["-path", "/migrations", "-database",  "postgres://postgres:root@postgres:5432/task-management-db?sslmode=disable", "force", "1"]
  #   links: 
  #       - postgres
  #   depends_on:
  #       - postgres

volumes:
  postgres-db:

networks:
  new:


