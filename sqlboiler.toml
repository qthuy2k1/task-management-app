output   = "models"
wipe     = true
no-tests = true
add-enum-types = true

[psql]
  host = "postgres"
  dbname = "task-management-db"
  port   = 5432
  user   = "postgres"
  pass   = "root"
  blacklist = ["migrations", "other"]
  sslmode = "disable"